# drawing-4950 NO BET Incident

## Impact

Drawing `4950`/`drawing-4950` (`drawing_id=11964`) finished with decision
`NO BET`.
No external cover/coupon was selected and no bank was spent.

## Root causes

1. **Initial matching regression:** raw matching produced `7/15` matches before reviewed aliases were included.
2. **Two provider-absent events:** two target pairs remain without matching provider events after strict matching constraints.
3. **`start_at` nullability:** TotoBrief supplied `start_at = null`, so raw effective timing could not be reconstructed without reviewed timing data.
4. **Self-dilution limit:** open EV bank is dynamically capped by exact 1% pool support.

## 1% cap and exact budget

Exact formula (implemented in effective budget logic):

`effective_budget = min(requested_bank, floor(pool_sum * 1% / stake) * stake)`

For drawing 4950: `pool_sum = 81_445`, `requested_bank = 4,980`, `stake = 30` -> `effective_budget = 810`.

## Matching and timing boundary outcome

- Matching in reviewed replay context reaches `13/15` after approved alias logic, while unresolved events stay fail-closed.
- Reviewed timing overlays are strict and must be catalog-validated with provenance parity at preflight/timing/package points. If checks fail, effective timing is `unknown`.
- With `timing = unknown` and strict policy, playable EV is suppressed and the final result is `NO BET`.

## Scheduler and manifest boundary

- Production flow is tracked at `T-45`, `T-30`, `T-15`, `T-10`.
- Fallback packages are diagnostic-only; only final freeze publication can produce a publishable package.
- Terminal markers: `.bet-ready`, `.no-bet`, `.failed`.
- Run-scoped package artifacts and hashes are immutable per run.
- Scheduler and runner manifests are strict-validated with schema/field and hash checks before package use.

## Verification status

- Full pytest: `1090 passed` (requested set).
- Targeted QA: `151 passed` (requested set).
- Ruff + `git diff --check`: passed.
- Final targeted verdict: `SAFE TO PROCEED` (operational boundary status).

No profitability claim is made from this incident.

## Historical note

Historical `drawing_4947` and early `drawing_4950` schema-v2 reports are pre-fix artifacts and are not current source-of-truth outputs.

## Pending

Live platform install and prospective live production run are still pending.
