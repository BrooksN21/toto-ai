# R3 fit — approved extraction, code frozen for review

Task TOTOAI-RESUME-20260915. 2026-09-15T21:43:08.842718+03:00

## Done / exact scope amendment

Owner/parent explicitly approved minimal shared A5 optimizer extraction from native wrapper. Added `v3_residual_kernel.py`; native wrapper delegates the original loop and uses extracted identical softmax. No domain/asof/label/class/eligibility/inference/release validation edits: exact source hashes/prefix checked. Eight original complete serialized model+prediction hashes remain byte-equal; research and native coefficients/transforms/predictions are also exactly equal on like-for-like synthetic fixtures.74focused tests PASS1.12s; Rufffive touched Pythonfiles PASS. Reference output frozen BEFORE source edit in R3-fit/pre-extraction-reference.json.

Research adapter now implements real `fit_research`, `freeze_predictions`, `score_predictions`; it no longer returns the earlier kernel-extraction blocker. Uses native train-only imputation, unchanged fixed L2.1/240steps/10s and shared optimizer. Distinct retrospective domain/non-operator artifacts. Reports genuine observed sport cells/active numeric sport coefficients separately from missing-indicator/intercept/BK coefficients; no blanket Sports integration claim.

Research-only denominator clarification explicitly authorized in data handoff: ≥45 REAL training rows across≥3earlier draws, not necessarily15 usable mappings each. Every missing slot must be in source-hash-bound exclusions; included+excluded complete15-slot denominators, duplicates/overlaps rejected. No fake kickoff/provider/feature rows. Native full15 admission remains unchanged. Training minimum counts actual included rows, NOT denominator. Model fitting may precede evaluation matrix via `heldout_pending=True`; prediction then requires separate exact reviewed features and rejects train/evaluation canonical fixture overlaps.

## Actual data / blocker (not a generic wait)

Data manifest f83552a06e5f896cacd0304b34b1b0e920dee9fdb0e975c43131aa67582c5d68 verified; all named R3 DTO hashes match.53serialized rows from4990/4992/4993/4995,60denominator,7explicit missing kickoff/fixture slots. Structural validation53/53passes.17numerical sport candidates, but all identity UNKNOWN /0reviewed positive weights. No genuine fit attempted, model hash/coefficients/predictions null. Synthetic nonzero coefficients are TEST evidence only. Independent identity review belongs to Huygens; code and DTO hashes must remain bound to its verdict. Never manufacture reviewed status by simply collecting all row hashes.

## Runnable API for next real fit

`fit_research(records, [], target_drawing=4999, prediction_as_of="2026-09-07T14:20:00+00:00", independently_reviewed_hashes=trusted_reviewed_feature_hashes, training_exclusions=exclusions, heldout_pending=True)`

`records` joins53separately sealed features/labels by drawing_number+canonicalevent_id, NOT arbitrary zip. `exclusions`: seven dictionaries with drawing_number,event_order,reason,source_sha256 bound to the verified readiness/roster evidence. Identity changes require reviewed derived artifacts preserving original hashes; no in-place changes to old inputs. Missing taxonomy staysnull, historical availability/fetch times unchanged.

Save sealed model BEFORE calling `freeze_predictions(model, heldout_rows, independently_reviewed_hashes=independent_eval_review_hashes)`. Save sealed prediction BEFORE loading evaluation labels into `score_predictions`. Eight4999–5006 previously seen outcomes remain retrospective diagnostics, not blind holdout or proof of edge. New inference only supports the exact research model/input hashes, not live native import.

All touched source/tests pinned in sibling JSON. No scheduler/DB/consent/sidecar/default registration/Git mutation. Current source cut is FROZEN for independent review; real fit is a separate subsequent execution after identity proof.
