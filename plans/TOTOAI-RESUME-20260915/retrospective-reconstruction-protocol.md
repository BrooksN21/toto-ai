# Retrospective reconstruction protocol v2 — executable first increment

Task TOTOAI-RESUME-20260915. PLANNING ONLY; no new generation, training, provider calls or activation performed. This addendum supersedes the old “missing market/pool makes5001–5006 impossible” assumption, not the live safety contract. Local gena-plan was absent in the three exact project skill locations; project-local totoai-backtesting and its canonical checklist were used. No global search/install/service call.

## 1. First assignment NOW: R1, outcome-blind closed-market scenario

**Owner:** one implementation worker, then separate independent review; no nested delegation. Implement only a research adapter/runner and focused tests. Do not wait for sports collection. Existing strict pre-draw RAW loader remains unchanged.

Inputs: `retrospective-reconstruction-context.json` rows give all8 exact RAW/meta paths and hashes. Use that bounded manifest, not latest mutable SQL quotes or another provider fetch. Extract BK/crowd/pool/jackpot/ordered event IDs into an explicit allowlisted generator DTO. Keep original capture timestamps and original bytes. Do not call these quotes verified closing quotes. The name “closed-market” refers only to finished drawings.

Suggested new scope (names are proposed, not existing executable commands):
- `src/toto_ai/research/closed_market_scenario_replay.py`
- `tests/test_closed_market_scenario_replay.py`
- reuse `research/native_quality_core.py` and existing native EV-surface builders; minimal pure binding helper if required, no live-entry refactor.
- outputs ONLY `reports/rehearsal/TOTOAI-RESUME-20260915/closed-market-scenario-v1/`.

**R1a acceptance before real execution:** validate8/120 RAW hashes/identity/order; freeze manifest/schema/code/config/seed/normalization and complete label-exclusion policy. Input domain `POST_EVENT_MARKET_SCENARIO_UNVERIFIED_ASOF`, `quote_available_at=null`, actual `captured_at` retained; `operator_compatible=false`, `automatic_wagering=false`, `fit_eligible=false`, `release_eligible=false`. No invented historical plan/consent/ledger or active/frozen-source status. Source classification is a new inactive research contract, not a new accepted live domain.

Bind EVSurface to the exact sanitized probability/crowd/pool/jackpot/config hashes; reject an unrelated caller-supplied surface. Reuse native stake/budget/objective math, not EV-topN/oracle substitutes. Native normalization must be explicit; optional norm-odds gaps cannot invent margin or replace BK. The original RAW has labels: only a bounded extraction step reads it; the generator gets only the sanitized DTO. Labels/scores/status/post-event winner information cannot flow through config, feature names, object references or a second RAW read during generation. This guards direct target leakage, NOT unknown quote chronology.

Small negative/contract suite: result/score mutation leaves generated inputs and deterministic output unchanged; changed market changes bound input; wrong RAW/meta/order/fixture/surface/config hash rejected; NaN/invalid triples rejected; optional missing odds stay missing; false predraw timestamp/domain upgrade rejected; mixed budgets/input rejected; output denied to operator/fit; duplicate robust reported; label scoring refuses unfrozen output. Reuse frozen4999 native parity fixture, report its exact scope rather than universal equivalence. Independent reviewer must confirm actual native family calls and RAW→surface binding before scenario execution.

**R1b actual run:** freeze all8 source manifests and one config before reading labels for scoring. Run4999 first as one bounded3-arm full-bank smoke (not the old780 test), then remaining5000–5006 sequentially. Every arm requests4980/30=166 on the SAME finished-source scenario; native pool cap/safety constraints remain. Context confirms finished pools numerically permit4980, but generation must assert actual effective bank/count. If fewer than166, preserve exact failure/partial count, do not duplicate coupons/pad/relax guards. Reuse completed verified outputs on resume by hash; no blind rerun.

Resource contract: one CPU-heavy drawing at a time; pilot allowance10min maximum, emit per-phase heartbeat to local progress JSON at most60s and parent-facing checkpoints at5min. Pilot records wall/CPU/RSS/candidate counts. Parent assigns subsequent bound from measured work and operational runway, maximum10min/draw until reviewed otherwise; timeout is PARTIAL with last phase, not “no model signal”. Do not silently trim algorithm search to fit a timer. No start beside imminent primary preparation; yield between drawings to parent operations. Eight-draw completion time is measured after pilot, not promised from the old139.53s/26coupon run.

Separate generation and scoring. Freeze/readback all available package/input hashes and completion statuses before batch scoring; scorer alone loads target labels. Reports contain eight rows × five requested family slots: three actual market arms plus two explicit `NOT_RUN_MISSING_SPORTS`/`NOT_RUN_NO_FIT`, never five fake successes. A three-arm scenario result is useful today even while full five-arm work remains open.

Proposed interface for implementation (NOT available/executed now):
```
.venv/bin/python -m toto_ai.research.closed_market_scenario_replay prepare --context plans/TOTOAI-RESUME-20260915/retrospective-reconstruction-context.json --bank 4980 --stake 30 --output reports/rehearsal/TOTOAI-RESUME-20260915/closed-market-scenario-v1
.venv/bin/python -m toto_ai.research.closed_market_scenario_replay generate --manifest <prepared-manifest> --drawing 4999 --max-seconds 600
# resume same command for5000..5006, frozen config/seed unchanged
.venv/bin/python -m toto_ai.research.closed_market_scenario_replay score --manifest <prepared-manifest> --frozen-generations <verified-batch-manifest> --labels <separate-label-manifest>
```
Executor reports actual implemented help/commands, targeted pytest/Ruff and review, then first3-arm receipt immediately; no need to wait for a polished full report.

## 2. Availability and evidence domains (never pool these results)

|Draw|Finished BK/crowd/results|Original captures|Sports available now|Scenario bank/count|
|---|---|---|---|---|
|4999|15/15/15; hashes verified|Archived4-model166 package and authentic predraw input|13-event source scope; accepted C adapter12/15, class unknown|4980/166 requested|
|5000|15/15/15; hashes verified|Genuine early pool78748:780/26 experiment, separate cohort|No drawing-specific sports runs|4980/166 on DIFFERENT finished pool|
|5001|15/15/15; hashes verified|No verified predraw market capture|No drawing-specific sports runs|4980/166 requested|
|5002|15/15/15; hashes verified|Same unknown quote as-of|No drawing-specific sports runs|4980/166 requested|
|5003|15/15/15; hashes verified|Same unknown quote as-of|No drawing-specific sports runs|4980/166 requested|
|5004|15/15/15; hashes verified|Same unknown quote as-of|No drawing-specific sports runs|4980/166 requested|
|5005|15/15/15; hashes verified|Same unknown quote as-of|No drawing-specific sports runs|4980/166 requested|
|5006|15/15/15; hashes verified|Same unknown quote as-of|No drawing-specific sports runs|4980/166 requested|

All8 finished payloads captured15Sep, no quote observation/change-log proof and no target kickoff values.4999/5000 finished market matrices differ from their latest active ones. Context inventory is the authority for exact timestamps/hashes, not this table. Missing snapshots prohibit faithful replay, not labeled computation.
- ARCHIVED_ACTUAL: original frozen packages, authentic inputs; actual wager/payout only with separately graded owner/broker evidence.
- POST_EVENT_MARKET_SCENARIO_UNVERIFIED_ASOF: computable diagnostic, no causal backtest/training/promotion claim. Sanitization does not make unknown-time quotes historically available.
- RECONSTRUCTED_HISTORICAL_RESEARCH: separate proposed admissibility contract with attributable old match facts and evidenced availability; late fetch retained. Uncertain revision/as-of rows remain excluded from causal fit or form a separately named sensitivity scenario, never silently eligible.
- PROSPECTIVE: only genuinely captured/frozen future experiments; SYNTHETIC only tests. No reconstructed/synthetic pass becomes production permission.
No ROI without payout evidence (all8 currently null). Freeze scenario choices now; labels already known to project means design-informed, not blind historical holdout.

## 3. R2 parallel sports reconstruction and reusable scope evidence

**Independent of R1; owner data worker plus independent scope reviewer.** First produce120-event availability ledger from supplied IDs and bounded exact joins into existing4999/5007 histories. Then fill5000–5006 using approved public sources in bounded per-draw collection jobs; not just rerun nonexistent saved sports snapshots. Initial pilot one exact target pair with dated history. Report known/unknown mappings, times, prior counts, missing features, source revision and class evidence for every event. Providers' last10 windows may be insufficient; test dated pagination once, otherwise exact gap—not an infinite regulation hunt.

Historical feature cutoff = chosen and explicitly sourced historical package-decision time, before close, NOT later target kickoff. Respect native Moscow interpretation of TotoBrief close, but never substitute close for missing kickoff/finish. Exclude target fixture, unfinished and later games, duplicate revisions and today's standings. Keep captured_at, claimed publication, updated_at, kickoff/finish, evidenced information_available_at and reviewer time distinct. Reconstruct season tables only from complete eligible season-to-cutoff ledger. Verify regulation90 for AET/PEN cases.

Use the existing **C-SCOPE-ENTITY-BINDING-V1 proposal**, still requiring explicit design acceptance/independent evidence review before implementation. One provider-team↔canonical-team + field-specific taxonomy + valid interval assertion can serve multiple matching rows, with immutable source/mapping/review hashes. No male/adult/first inference from names, no blind inheritance from target/opponent/league, no invented publication date, no self-approved source boolean. Partial/contradictory class stays unknown/rejected. Legal regulations for every match are not required. Preserve all17 proposed negative checks plus original gate tests.

Exit: a120-row ledger with exact per-feature evidence/missingness and per-draw15-slot feature snapshots; partial snapshots allowed as data outputs but not silently fit-ready folds. Original sources unchanged, no live DB writes from reconstruction. Per-draw proof/status enables sports-shadow and V3 separately; no blanket assertion that all history is unavailable or already reconstructed.

## 4. R3 dataset → fit → predictions (Sports v3 remains unfitted today)

Dependencies: accepted scoped taxonomy/identity/time evidence and a reviewed reconstructed-domain adapter; A's adopted library alone is not trained Sports v3. Proposed domain support belongs in isolated research orchestration/admission, not global weakening of `FROZEN_HISTORICAL_INPUT`/production validators. Independently review and version it before fit. Unknown-asof BK/crowd/pool cannot enter a BK-centered model's causal fit. If historic market evidence is unavailable, either explicitly scenario-grade the entire fit/evaluation or block causal fit; do not rename a different sports-only model as accepted V3.

Before outcomes enter any fitter, seal one dataset manifest: original/derived source hashes,15 ordered target rows per draw, feature schema37/53-family disposition, predictor/label separate hashes, exclusions, admissibility evidence and chronological fold roster. Require earlier eligible existing history (before4999) for4999 training; audit this bounded eligible roster, not all project history. No earlier eligible corpus => UNTRAINED/INSUFFICIENT_TRAINING, not invented coefficients.

Predeclare4999–5003 as design/development diagnostics; freeze5004–5006 as no-further-tuning historical confirmation set. All8 outcomes have been accessible, so this is NOT a pristine unseen holdout. For confirmation freeze architecture/hyperparameters/transforms at the4999–5003 boundary; no fitting/imputation/calibration on5004–5006 labels. Development walk-forward uses only strictly earlier whole draws with evidenced label availability before each historical decision cutoff. A later expanding-window research experiment must be separately named, never retroactively described as this frozen confirmation. Obtain truly untouched evidence on future prospective draws before improvement claims.

Fixed BK-centered regularized residual, sum-zero/L2 constraints, train-only transforms/imputation, weight and L1 caps remain. Retain original A0–A5 control/ablation definitions. Extend legacy≤7fold evaluation through a versioned eight-slot research roster, preserving legacy tests/gates and every missing/failed draw; no cherry-picked7fold pass. Report actual feature coverage, not old105-event denominators; missingness does not disappear through imputation.

Fit acceptance: model version/coefficients/train-target IDs/source hashes/config/fold cutoffs saved; labels ofheldout/current/future targets cannot alter earlier predictions (negative tests). Prediction acceptance:15event vectors finite/nonnegative/sum1, aligned IDs/order, correct BK reference, verified scope/reliability, explicit fallback reasons, caps enforced, reload deterministic. Freeze artifacts BEFORE scoring. No-fit/fallback-only arm explicitly not a trained-model result. Dataset/train manifests and independent leakage review mandatory; execution bound/pilot measurement before any long fit.

## 5. R4 full five-way comparison, then R5 protected sidecar

Five arms are predictor×optimizer, not five independent probability models:
1. BK × native quality-v2 control.
2. BK × native quality-v3.
3. market-only native robust initially; later full sports-composed robust is a DIFFERENT typed arm/config, not an invisible upgrade.
4. SportsV2 × exact saved sports-shadow configuration (pin real optimizer and caps from evidence).
5. genuinely FITTED SportsV3 × quality-v2 fixed optimizer, isolating predictor effect; cross-optimizer V3 variants later as explicit ablations.

All eligible arms per comparison share exact base market/event snapshot, stake30/effective4980/count166, native budget/EV configuration, objective definition, seeds and evaluation scenarios. Different fitted predictor matrices are intentional and individually hashed; never pretend they are identical inputs. Report common frozen BK-reference package probability separately from each self-model probability. Sports missingness/fallback rates and actual trained model IDs visible. Actual archived results, genuine780/26 replay and finished4980/166 scenario have separate tables, never pooled as equivalent performance.

Measure: multiclass logloss/sumBrier and draw-class calibration for each distinct predictor; category13/14/15 union probability under same reference measure (not sum of overlapping coupon chances), actual max/mean hits and9–15 counts, draw-level hit frequency, diversity/concentration, unique coupons, fallback/no-op reason, CPU/RSS/runtime and effective bank. Shared event independence assumption explicit; fixed/common sampling if exact probability evaluation is bounded out, with MC uncertainty. Paired differences by draw with uncertainty;8draws are tiny diagnostics, not demonstrated lift/profit. ROI null unless attributable payout evidence.

R5 only after independent dataset/fit/inference/comparison review: isolated shadow candidate behind existing sidecar flag/manifest, operator_compatible=false until ordinary release contract independently accepts it. Primary publishes FIRST; missing/slow/invalid sidecar fails open to primary and cannot take its resource budget/locks or delay final delivery. No default scheduler change in research jobs. Test source/plan/bank/hash mismatch, timeout/crash, label input, wrong model domain, no-fit and rejected scope. Fresh plan-bound consent, same-final-input selector and current release gates remain; historical hit counts never authorize activation or PLAY.

## 6. Improvement backlog, in priority order, not all-at-once implementation

P0: binding/equal-comparison and native equivalence; distinguish optimizer no-op from fallback/timeout. No speed shortcut that changes semantics without parity test.
P1: Sports coverage/identity/chronology, fit real V3 and validate calibration/logloss versus BK; SportsV2 bounded residual/reliability ablations on earlier data only.
P2: quality-v3 diversity/concentration and13+/14+/15 trade-off using frozen reference metrics; quality-v2 retains unchanged control, calibration a separate versioned candidate. Do not optimize to these8 realized outcomes.
P3: robust stress scenarios/uncertainty bounds and candidate-vs-control reasoned trace, measuring worst-case objective as well as common probability; identical package is not improvement.
P4: probability/provider calibration and multi-model combinations only after unique predictor evidence; broader untouched prospective sample before promotion or larger-bank recommendations. No promise of13/15 or profit.

## Acceptance/report cadence

Each implementation receipt: DONE/RUNNING/REMAINS/BLOCKER/NEXT, exactpaths, source/config/output hashes, focused tests/Ruff and independent review scope. First next receipt = R1a runnable adapter + focused tests; immediately after review R1b first4999 three-arm166 run, then8-draw batch progress. R2 starts independently, R3–R5 conditional on explicit artifacts—not calendar promises. This planning job runs NONE of those commands and makes no publication.
