# Sports V3 probability research implementation — review pending

An isolated implementation now supports train → sealed artifact → infer and an
explicit default-off comparison API hook. It does not replace Sports V2, the
primary package, the four-strategy selector or operator consent. No CLI activation,
real trained 4998 package, F4 acceptance or profitability claim is established.

The saved contract remains centered BK logits plus a sum-zero L2 residual,
reliability capped at0.20 and per-event L1 displacement capped at0.20. There are
37 core predictors (accepted28 plus9 differences) and53 full predictors. A2/A3
requires complete core; A4/A5 uses train-only imputation/missing indicators.
Optional standings requires exact league/season/team/time; rolling/rest histories
may cross competitions/seasons. Exact common entity, capture, chronology,
regulation-90 and source-rejection rules are unchanged.

Snapshot03 corrections address independent review findings: frozen generator input
is bound as a whole, including financial configuration and ordered BK, to inference;
serialized training manifests are validated for counts/IDs/earlier labels/config.
Integration requires independently supplied expected model/training/manifest hashes.
Those hashes are bindings, not F4 or operator authority. Do not derive expected
hashes from an unreviewed artifact and call that approval.

The six-draw bound diagnostic preserves90 labels,30 chronology-rejected rows and8
additional missing-history rows.52 rows have source-valid histories;48 have all37
core predictors, while4 lack home venue features and derived venue differences.
Independent-opponent/standings/BK-margin features are absent. No independent reviewed
common entity-scope proof is supplied, so training remains unauthorized. The older
forced scope=false fit is not evidence that all feature families were rejected.

Native new-coupon generation is demonstrated with synthetic data only. Full F4
chronological ablation/bootstrap/calibration gates, production quality-v2 provenance,
independent candidate safety/exposure checks and remaining adversarial hardening
are not complete. Research output is never an uploadable or wagerable package.

Resume from the immutable SPORTS_V3_PROBABILITY_CORE_SNAPSHOT_03 manifest and
SPORTS_V3_PROBABILITY_HANDOFF_03 in the plan directory. No main/runtime changes.
